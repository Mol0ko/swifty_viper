//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest

class {{ module_info.name }}AssemblyTests: XCTestCase {

    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

    func testCreacteModule() {

        // given
        let assembly = {{ module_info.name }}Assembly()

        // when
        let viewController = assembly.createModule()

        // then
        guard let presenter = viewController.output as? {{ module_info.name }}Presenter else {
            XCTFail("ViewOutput is not {{ module_info.name }}Presenter")
            return
        }

        XCTAssertNotNil(presenter.view, "view in {{ module_info.name }}Presenter is nil after configuration")

        guard let router = presenter.router as? {{ module_info.name }}Router else {
            XCTFail("RouterInput is not {{ module_info.name }}Router")
            return
        }
        XCTAssertNotNil(router.view, "view in {{ module_info.name }}Router is nil after configuration")

        guard let interactor = presenter.interactor as? {{ module_info.name }}Interactor else {
            XCTFail("InteractorInput is not {{ module_info.name }}Interactor")
            return
        }
        XCTAssertNotNil(interactor.output, "output in {{ module_info.name }}Interactor is nil after configuration")
    }
}
